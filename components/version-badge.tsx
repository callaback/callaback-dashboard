// components/version-badge.tsx
//   build-{version}

export function VersionBadge({ version }: { version: string }) {
  return (
    <div className="pointer-events-none fixed bottom-2 left-6 -translate-x-1/2 text-[10px] text-muted-foreground/60 select-none">
 
    </div>
  )
}
